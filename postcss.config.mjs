/** Local PostCSS config — overrides any config found in parent directories.
 *  This project uses plain CSS (no Tailwind), so no plugins are needed. */
const config = {
  plugins: {},
};

export default config;
