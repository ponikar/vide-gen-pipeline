// Local PostCSS config so Next.js does not inherit a Tailwind config from a
// parent directory. The site uses plain CSS (with manual vendor prefixes),
// so no PostCSS plugins are required.
const config = {
  plugins: {},
};

export default config;
