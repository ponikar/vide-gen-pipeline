# Instagram App Credentials (used server-side on Vercel)
INSTAGRAM_APP_ID=
INSTAGRAM_APP_SECRET=

# Database (Neon/Vercel Postgres — auto-injected on Vercel, set manually for local migration)
POSTGRES_URL=

# Vercel API (for CLI — local dev or production URL)
VERCEL_API_URL=http://localhost:3000
